import React from "react";

export default function NPhaseBio() {
    return (
        <div>
            <h1>Bios</h1>
            {/* Add services carousel here */}
            {/* Animate it to slide in from the left */}
            {/* TODO: Figure out how to do a carousel without bootstrap.
            Can I use Bootstrap on just one page? */}
        </div>
    );
}